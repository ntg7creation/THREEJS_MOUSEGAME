/**
 * Object containing predefined colors. its a const so the js code can also use it
 */
export const Colours = {
  RED: '#FF0000',
  GREEN: '#00FF00',
  BLUE: '#0000FF'
};

/**
 * Enum representing different shapes.
 */
export enum Shapes {
  Box,    // 0
  Sphere,  // 1
  Pyramid    // 2
}